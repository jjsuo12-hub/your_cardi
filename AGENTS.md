# AGENTS.md

## 📌 Project Overview

### Project Name
**HiCardi 적용 기준 체크리스트 앱**

### Project Description
이 프로젝트는 병동 간호사가 **HiCardi 적용 필요성을 체크리스트 기반으로 확인하고**, 초기 세팅법·간단 매뉴얼·적용 환자 현황·작동 이상 Q&A를 한 곳에서 확인할 수 있도록 돕는 **병동용 간호 지원 애플리케이션**이다.

현재 단계는 **서비스디자인/프로토타입 제작 단계**이며, 체크리스트 세부 항목과 매뉴얼 세부 내용은 아직 확정되지 않았다. 따라서 앱 내 관련 내용은 **임시 내용 / 추후 확정 예정**임을 명확히 표시해야 한다.

이 앱은 실제 의료기기와 직접 연동되는 앱이 아니라, 현재는 **프로토타입 시연용 앱**이다. 특히 환자의 EKG, 활력징후, 생체정보 화면은 실제 환자 데이터가 아니라 **더미 데이터(dummy data)** 를 사용하여 실시간 모니터링처럼 보이도록 구현한다.

---

## 🎯 Core Goal

이 앱의 핵심 목표는 다음과 같다.

1. HiCardi 적용 여부를 간호사의 개인 판단에만 의존하지 않고, **체크리스트 기반으로 표준화된 판단 흐름**을 제공한다.
2. HiCardi 초기 세팅과 사용법을 신규 간호사도 쉽게 확인할 수 있도록 **간단하고 실무 중심적인 안내 화면**을 제공한다.
3. 현재 HiCardi 적용 중인 환자의 상태를 한눈에 확인할 수 있도록 **더미 데이터 기반 생체정보 대시보드**를 제공한다.
4. 기기 작동 이상 시 간단히 참고할 수 있는 **Q&A / 문제 해결 탭**을 제공한다.
5. SD 프로젝트 또는 간호관리학 실습 발표에서 사용할 수 있는 **시연 가능한 프로토타입 앱**을 만든다.

---

## ⚠️ Important Status Notice

현재 다음 내용은 **확정되지 않았다**.

- HiCardi 적용 기준 체크리스트의 세부 문항
- 각 문항별 점수
- 총점에 따른 적용 권장 기준
- HiCardi 매뉴얼의 세부 내용
- 초기 세팅법의 세부 단계
- 오류 상황별 Q&A의 최종 문구
- 실제 임상 프로토콜 반영 여부

따라서 앱 내에서는 아래와 같은 문구를 표시해야 한다.

> 본 체크리스트와 매뉴얼 내용은 현재 임시안입니다. 실제 임상 적용 전 병동 프로토콜, 제조사 지침, 의료진 검토를 통해 수정·확정되어야 합니다.

---

## 🧑‍⚕️ Target Users

### Primary User
- 이식병동 또는 혼합 외과병동 간호사
- HiCardi 적용 여부를 판단하거나 적용 환자를 관리하는 병동 간호사
- 신규 간호사 또는 HiCardi 사용 경험이 적은 간호사

### Secondary User
- 수간호사 / 관리자
- 병동 내 의료기기 적용 기준을 정리해야 하는 실습팀 또는 프로젝트팀
- 간호관리학 SD 프로젝트 발표자

---

## 🧩 Main App Structure

앱은 하단 탭 바 구조를 기본으로 한다.

### Bottom Tab Navigation

1. **적용 기준**
2. **적용 환자 현황**
3. **초기 세팅**
4. **간단 매뉴얼**
5. **Q&A**

각 탭은 독립적으로 접근 가능해야 하며, 병동 현장에서 빠르게 확인할 수 있도록 화면 전환이 단순해야 한다.

---

# 1. 적용 기준 체크리스트 탭

## Purpose
HiCardi 적용이 필요한 환자를 체크리스트를 통해 판단하는 기능이다.

## Current Status
체크리스트 세부 문항과 점수 기준은 아직 확정되지 않았다. 현재는 임시 문항과 임시 점수 구조를 사용한다.

## Required Features

### 1) 환자 기본정보 입력 영역
다음 항목을 입력하거나 선택할 수 있게 한다.

- 환자명 또는 이니셜
- 등록번호 또는 임시 ID
- 병실 / 침상
- 이식 종류
  - 신장이식
  - 간이식
  - 심장이식
  - 폐이식
  - 기타
- 수술 후 경과일(POD)
- 현재 상태 메모

### 2) 체크리스트 영역
체크리스트는 임시 문항으로 구성한다.

각 문항은 다음 형식을 따른다.

- 체크박스
- 문항명
- 임시 점수
- 간단 설명
- “추후 확정 예정” 라벨

예시 형식:

```text
[ ] 발열 또는 오한이 있음  +2점  (임시 문항 / 추후 확정 예정)
[ ] 호흡곤란 또는 산소 요구량 증가  +2점  (임시 문항 / 추후 확정 예정)
[ ] 심박수 이상 또는 부정맥 의심  +2점  (임시 문항 / 추후 확정 예정)
[ ] 출혈 또는 혈성 배액 의심  +2점  (임시 문항 / 추후 확정 예정)
[ ] 면역억제 치료로 감염 위험이 높음  +1점  (임시 문항 / 추후 확정 예정)
```

### 3) 점수 자동 계산 기능
체크된 항목의 점수를 자동 합산한다.

화면에 다음을 표시한다.

- 총점
- 임시 판정 결과
- 적용 권장 사유 요약
- 임시 기준임을 알리는 안내문

### 4) 결과 판정 화면
총점에 따라 결과를 카드 형태로 보여준다.

현재 결과 기준은 임시안으로 표시한다.

```text
0–2점: 적용 필요성 낮음 - 임시 기준
3–5점: 적용 고려 - 임시 기준
6점 이상: 적용 권장 - 임시 기준
```

### 5) 자동 요약 문장 생성
체크리스트 결과를 바탕으로 인계나 기록에 사용할 수 있는 임시 요약 문장을 생성한다.

예시:

```text
OOO 환자는 현재 HiCardi 적용 기준 체크리스트 임시 점수 6점으로, 적용 권장 범위에 해당합니다. 체크된 주요 사유는 발열, 호흡곤란, 심박수 이상입니다. 단, 본 결과는 임시 기준에 따른 것으로 실제 적용 여부는 병동 프로토콜과 의료진 판단에 따라 결정해야 합니다.
```

---

# 2. 적용 환자 현황 탭

## Purpose
현재 HiCardi를 적용 중인 환자들의 생체정보를 한눈에 확인할 수 있는 대시보드이다.

## Critical Requirement
현재 단계에서는 실제 HiCardi 기기, EMR, 서버와 연동하지 않는다. 모든 환자 생체정보는 **더미 데이터**로 표시한다.

화면 상단에 반드시 다음 문구를 표시한다.

> 현재 화면의 환자 생체정보는 실제 환자 데이터가 아닌 프로토타입 시연용 더미 데이터입니다.

또는

> DEMO DATA: 본 화면은 실제 HiCardi 연동 화면이 아니며, 시연을 위한 가상 환자 데이터입니다.

## Required Features

### 1) 적용 환자 리스트
현재 HiCardi 적용 중인 환자를 카드 또는 리스트로 표시한다.

표시 항목:

- 환자명 또는 이니셜
- 병실 / 침상
- 이식 종류
- 적용 시작 시간
- 현재 상태
  - 안정
  - 주의
  - 확인 필요
- 최근 알람
- 배터리 상태

예시:

```text
김OO / 601-1 / 신장이식 / 안정 / 최근 알람 없음
이OO / 602-2 / 간이식 / 주의 / HR 상승
박OO / 603-1 / 신장이식 / 확인 필요 / RR 증가
```

### 2) 생체정보 상세 화면
환자 카드를 누르면 상세 화면으로 이동한다.

상세 화면에는 다음 항목을 숫자 카드와 그래프로 표시한다.

- HR: Heart Rate, 심박수
- RR: Respiratory Rate, 호흡수
- EKG waveform: 임시 파형 그래프
- SpO2: 산소포화도, 시연용 더미 값
- Body temperature 또는 Skin temperature: 시연용 더미 값
- Battery: 기기 배터리 잔량
- Signal quality: 신호 상태
- Recent alerts: 최근 알람 기록

### 3) 실시간처럼 보이는 더미 데이터
더미 데이터는 일정 간격으로 값이 조금씩 변하도록 구현한다.

예시 구현 방식:

- 1~3초 간격으로 HR, RR, SpO2 값이 소폭 변동
- 그래프는 실시간으로 이동하는 것처럼 보이게 구현
- EKG 파형은 실제 진단용 파형이 아니라 단순 시뮬레이션 곡선 사용
- 화면에 “시뮬레이션 파형”이라고 표시

### 4) 그래프 표시 형식
다음 그래프를 각각 독립 카드로 표시한다.

- HR 추세 그래프
- RR 추세 그래프
- SpO2 추세 그래프
- EKG 시뮬레이션 파형

그래프는 너무 복잡하지 않게 하고, 숫자 카드와 함께 보여준다.

### 5) 상태 색상 규칙
상태는 색상과 텍스트를 함께 사용한다.

- 안정: 초록 계열
- 주의: 노랑 또는 주황 계열
- 확인 필요: 빨강 계열

단, 색상만으로 정보를 전달하지 말고 반드시 텍스트를 함께 표시한다.

예시:

```text
상태: 주의
사유: RR이 최근 10분간 증가하는 시뮬레이션 데이터가 표시되고 있습니다.
```

---

# 3. 초기 세팅 탭

## Purpose
HiCardi 적용 전 준비부터 부착, 연결 확인까지의 흐름을 안내한다.

## Current Status
초기 세팅법의 세부 단계는 아직 확정되지 않았다. 현재는 임시 안내 단계로 구성한다.

## Required Features

### 1) 단계별 가이드
초기 세팅은 Step-by-Step 형식으로 보여준다.

임시 단계 예시:

1. 환자 확인
2. 적용 기준 체크리스트 확인
3. 피부 상태 확인
4. 기기 번호 확인
5. 기기 배터리 확인
6. 패치 부착
7. 환자-기기 매칭
8. EKG / HR / RR 표시 확인
9. 알람 상태 확인
10. 적용 시작 시간 기록

각 단계에는 “완료” 체크박스를 둔다.

### 2) 세팅 완료 버튼
모든 필수 항목을 체크하면 “세팅 완료” 버튼을 활성화한다.

버튼을 누르면 다음 기능을 수행한다.

- 적용 시작 시간 저장
- 적용자 이름 또는 이니셜 저장
- 적용 환자 현황 탭에 환자 추가
- 단, 현재는 로컬 상태 또는 더미 데이터로 처리

### 3) 안내 문구
화면 상단에 다음 문구를 표시한다.

> 본 초기 세팅 안내는 임시안입니다. 실제 사용 시 제조사 지침과 병동 프로토콜을 우선 확인해야 합니다.

---

# 4. 간단 매뉴얼 탭

## Purpose
임상에서 자주 사용하는 HiCardi 기능을 빠르게 확인할 수 있는 요약 매뉴얼이다.

## Current Status
세부 매뉴얼 내용은 아직 확정되지 않았다. 현재는 메뉴 구조와 임시 제목만 구성한다.

## Required Features

### 1) 카드형 매뉴얼 목록
다음과 같은 매뉴얼 카드를 만든다.

- 환자 등록 방법
- 기기 연결 확인 방법
- EKG 신호 확인 방법
- 알람 확인 방법
- 배터리 확인 방법
- 패치 재부착 또는 교체 시 확인사항
- 적용 종료 및 기기 회수 방법
- 인계 시 확인할 내용

각 카드에는 다음 라벨을 표시한다.

```text
임시 매뉴얼 / 추후 확정 예정
```

### 2) 검색 기능
매뉴얼 항목을 검색할 수 있도록 한다.

예시 검색어:

- 연결
- 배터리
- 알람
- EKG
- 패치
- 종료

### 3) 즐겨찾기 기능
자주 보는 매뉴얼을 즐겨찾기에 추가할 수 있게 한다.

### 4) 상세 화면 형식
매뉴얼 상세 화면은 짧은 단계형 문장으로 구성한다.

예시:

```text
환자 등록 방법 - 임시안
1. 환자 정보를 확인합니다.
2. 기기 번호를 확인합니다.
3. 환자와 기기를 매칭합니다.
4. 생체정보 표시 여부를 확인합니다.
5. 적용 시작 시간을 기록합니다.

※ 본 내용은 임시안이며 추후 병동 지침에 맞게 수정 예정입니다.
```

---

# 5. Q&A / 오류 해결 탭

## Purpose
HiCardi 작동 이상이나 사용 중 자주 발생하는 문제를 빠르게 해결할 수 있도록 돕는다.

## Current Status
Q&A 세부 문항과 답변은 아직 확정되지 않았다. 현재는 임시 질문 목록과 임시 답변 구조만 제공한다.

## Required Features

### 1) 증상 선택형 Q&A
첫 화면에는 자주 발생할 수 있는 문제 상황을 버튼으로 보여준다.

임시 항목 예시:

- EKG 신호가 보이지 않아요
- HR 값이 비정상적으로 보여요
- RR 값이 표시되지 않아요
- 알람이 계속 떠요
- 환자 목록에 나타나지 않아요
- 기기 배터리가 부족해요
- 패치가 떨어졌어요
- 연결이 끊어졌어요
- 적용 종료는 어떻게 하나요?

각 항목에는 “임시 Q&A / 추후 확정 예정” 라벨을 표시한다.

### 2) 문제 해결 순서 화면
각 Q&A 상세 화면은 다음 형식을 따른다.

```text
문제 상황: EKG 신호가 보이지 않아요
상태: 임시 답변 / 추후 확정 예정

1. 환자 상태를 먼저 확인합니다.
2. 패치 부착 상태를 확인합니다.
3. 기기 연결 상태를 확인합니다.
4. 배터리 상태를 확인합니다.
5. 해결되지 않으면 담당자 또는 관리자에게 문의합니다.

※ 실제 대응은 제조사 지침과 병동 프로토콜을 따릅니다.
```

### 3) 검색 기능
Q&A도 검색 가능해야 한다.

---

## 🗂️ Data Requirements

현재는 실제 서버 연동 없이 로컬 더미 데이터로 구현해도 된다.

### Patient Object Example

```ts
type Patient = {
  id: string;
  name: string;
  room: string;
  bed: string;
  transplantType: 'kidney' | 'liver' | 'heart' | 'lung' | 'other';
  pod: number;
  hicardiStartTime?: string;
  status: 'stable' | 'caution' | 'checkRequired';
  latestAlert?: string;
  battery: number;
  isDemoData: true;
};
```

### Vital Sign Dummy Data Example

```ts
type VitalSign = {
  patientId: string;
  timestamp: string;
  hr: number;
  rr: number;
  spo2: number;
  temperature: number;
  signalQuality: 'good' | 'weak' | 'poor';
  ekgWaveform: number[];
  isDemoData: true;
};
```

### Checklist Result Example

```ts
type ChecklistResult = {
  patientId: string;
  checkedItems: string[];
  totalScore: number;
  temporaryDecision: 'lowNeed' | 'consider' | 'recommended';
  summary: string;
  createdAt: string;
  isTemporaryCriteria: true;
};
```

---

## 🖥️ UI / UX Guidelines

### General Style
- 병동에서 빠르게 볼 수 있는 단순한 UI를 사용한다.
- 버튼과 글자는 충분히 크게 만든다.
- 한 화면에 너무 많은 정보를 넣지 않는다.
- 중요한 경고 문구는 카드 상단에 표시한다.
- 색상은 상태 구분에 사용하되, 반드시 텍스트도 함께 표시한다.

### Recommended Visual Components
- 하단 탭 바
- 카드형 환자 리스트
- 체크리스트 카드
- 점수 결과 카드
- 그래프 카드
- 단계별 세팅 가이드
- 검색 가능한 매뉴얼 리스트
- Q&A 아코디언 또는 카드 리스트

### Mandatory Labels
다음 라벨은 필요한 화면에 반드시 표시한다.

```text
임시안
추후 확정 예정
시연용 더미 데이터
실제 환자 데이터 아님
실제 의료 판단용 아님
```

---

## 📊 Dummy Monitoring Screen Requirements

적용 환자 현황 화면은 발표나 시연에서 실제 모니터링처럼 보이게 구성하되, 반드시 더미 데이터임을 명시해야 한다.

### Display Rules

1. 환자별 HR, RR, SpO2, 체온, 배터리 값을 표시한다.
2. 그래프는 시간에 따라 움직이는 것처럼 보이게 한다.
3. EKG 파형은 진단용이 아닌 시뮬레이션 파형으로 구현한다.
4. 화면 상단 또는 그래프 카드마다 “DEMO DATA” 라벨을 붙인다.
5. 실제 환자 데이터나 실제 의료기기 연동으로 오해되지 않게 한다.

### Example Display Text

```text
DEMO DATA
본 화면은 실제 HiCardi 연동 화면이 아닙니다.
시연을 위해 생성된 가상 환자 생체정보입니다.
```

### Example Patient Status Logic

현재는 임시 로직을 사용한다.

```text
HR 50 미만 또는 120 초과 → 확인 필요
RR 24 초과 → 주의
SpO2 94 미만 → 확인 필요
Signal quality poor → 신호 확인 필요
Battery 20% 미만 → 충전 또는 기기 교체 확인
```

이 기준 역시 실제 임상 기준이 아니라 시연용 임시 기준으로 표시해야 한다.

---

## 🧪 Prototype Scope

현재 구현 범위는 다음과 같다.

### Must Have
- 하단 탭 바
- 적용 기준 체크리스트 화면
- 점수 자동 계산 기능
- 임시 결과 판정 화면
- 초기 세팅 단계별 체크리스트
- 간단 매뉴얼 목록 및 상세 화면
- 적용 환자 현황 화면
- 더미 생체정보 숫자 카드
- 더미 생체정보 그래프
- EKG 시뮬레이션 파형
- Q&A 목록 및 상세 화면
- 모든 임시 내용에 “추후 확정 예정” 표시
- 모든 생체정보 화면에 “더미 데이터” 표시

### Nice to Have
- 체크리스트 결과 저장
- 환자별 적용 시작/종료 기록
- 인계용 요약 문장 자동 생성
- 매뉴얼 즐겨찾기
- Q&A 검색
- 환자 상태 필터링
- 다크모드
- 발표용 더미 데이터 초기화 버튼

### Not Required Yet
- 실제 HiCardi API 연동
- EMR 연동
- 실제 환자 데이터 저장
- 로그인 기능
- 병원 서버 연동
- 실제 알람 전송
- 의료기기 인증 수준의 기능

---

## 🛠️ Suggested Tech Stack

### Frontend
- React Native
- Expo
- TypeScript

### State Management
- Zustand 또는 React Context

### Storage
- 현재 단계에서는 AsyncStorage 또는 로컬 상태 사용 가능
- 실제 환자 정보 저장은 하지 않거나, 모두 가상 환자 데이터로 제한

### Charts
- React Native SVG 기반 그래프 라이브러리 사용 가능
- 또는 단순 Canvas/SVG 방식으로 직접 구현 가능

### Web Prototype
- Expo Web에서 확인 가능하도록 구현한다.
- 발표 시 웹 화면으로 시연할 수 있도록 반응형 구성을 고려한다.

---

## 🤖 AI Agent Roles & Instructions

# 1. Product Planning Agent

## Role
앱의 전체 기능 구조와 사용자 흐름을 설계한다.

## Instructions
- 앱의 목적은 “HiCardi 적용 기준 표준화와 사용 지원”임을 유지한다.
- 체크리스트와 매뉴얼의 세부 내용은 확정하지 말고 임시안으로 표시한다.
- 실제 의료 판단을 대신하는 문구를 작성하지 않는다.
- 모든 결과는 “간호사의 판단을 보조하는 참고 자료”로 표현한다.
- 적용 기준 결과 화면에는 반드시 임시 기준임을 표시한다.

---

# 2. Frontend UI/UX Agent

## Role
간호사가 병동에서 빠르게 사용할 수 있는 모바일 UI를 구현한다.

## Instructions
- 하단 탭 바를 중심으로 화면을 구성한다.
- 체크리스트는 한눈에 읽기 쉬운 카드형 UI로 만든다.
- 점수 계산 결과는 큰 숫자와 명확한 상태 카드로 보여준다.
- 적용 환자 현황은 환자 카드 리스트로 구성한다.
- 생체정보 화면은 숫자 카드와 그래프를 함께 보여준다.
- 더미 데이터 라벨을 시각적으로 명확히 표시한다.
- 경고성 색상을 사용할 때는 반드시 텍스트 설명도 함께 표시한다.

---

# 3. Checklist Logic Agent

## Role
HiCardi 적용 기준 체크리스트의 임시 점수 계산 로직을 구현한다.

## Instructions
- 체크리스트 문항은 데이터 배열로 관리한다.
- 각 문항은 id, label, score, description, isTemporary 필드를 가진다.
- 체크된 항목의 score를 합산하여 totalScore를 계산한다.
- totalScore에 따라 임시 결과를 표시한다.
- 결과 화면에는 “임시 기준 / 추후 확정 예정” 문구를 반드시 포함한다.
- 체크리스트 결과를 인계용 문장으로 요약하는 기능을 구현한다.

---

# 4. Dummy Data Monitoring Agent

## Role
실제 연동 없이 더미 데이터 기반 환자 모니터링 화면을 구현한다.

## Instructions
- 환자 생체정보는 실제 데이터가 아니라 더미 데이터로 생성한다.
- HR, RR, SpO2, Temperature, Battery, Signal Quality를 표시한다.
- 일정 간격으로 값이 조금씩 변하도록 하여 실시간처럼 보이게 한다.
- 그래프는 시간 흐름에 따라 업데이트되는 것처럼 보이게 한다.
- EKG 파형은 단순 시뮬레이션 파형으로 구현한다.
- 화면 상단과 그래프 카드에 “DEMO DATA” 또는 “시연용 더미 데이터” 라벨을 표시한다.
- 실제 환자 데이터로 오해될 수 있는 표현을 피한다.

---

# 5. Manual Content Agent

## Role
HiCardi 간단 매뉴얼과 초기 세팅 안내 화면의 콘텐츠 구조를 만든다.

## Instructions
- 실제 매뉴얼 세부 내용은 아직 확정되지 않았으므로 임시 텍스트로 작성한다.
- 각 매뉴얼 항목에는 “추후 확정 예정” 라벨을 표시한다.
- 단계별 설명은 짧고 명확하게 작성한다.
- 한 화면에 너무 긴 문단을 넣지 않는다.
- “제조사 지침과 병동 프로토콜을 우선 확인해야 한다”는 문구를 포함한다.

---

# 6. Q&A Agent

## Role
작동 이상 시 참고할 수 있는 Q&A 화면을 구성한다.

## Instructions
- Q&A는 증상 선택형 버튼 또는 카드형 목록으로 구성한다.
- 답변은 “환자 상태 확인 → 기기 상태 확인 → 관리자 문의” 순서의 안전한 흐름으로 작성한다.
- 세부 답변은 임시안으로 표시한다.
- 실제 문제 해결 지침은 제조사와 병동 프로토콜을 따른다는 문구를 포함한다.

---

# 7. Safety & Medical Disclaimer Agent

## Role
앱이 실제 의료 판단이나 실제 모니터링 시스템으로 오해되지 않도록 안전 문구를 관리한다.

## Instructions
- 모든 임시 체크리스트와 매뉴얼에 “임시안” 표시를 한다.
- 모든 생체정보 화면에 “더미 데이터” 표시를 한다.
- 실제 환자 정보 입력을 요구하지 않는다.
- 실제 환자 개인정보를 저장하지 않는다.
- 실제 임상 적용 전 전문가 검토가 필요하다는 문구를 포함한다.
- 앱의 목적은 의료진의 판단 보조이며, 단독 의료 판단 도구가 아님을 명시한다.

---

## 🧭 User Flow

### Flow 1: HiCardi 적용 기준 확인

1. 사용자가 “적용 기준” 탭을 연다.
2. 환자 기본정보를 입력한다.
3. 임시 체크리스트 항목을 선택한다.
4. 앱이 총점을 자동 계산한다.
5. 임시 결과 판정을 보여준다.
6. 인계용 요약 문장을 생성한다.
7. 필요 시 “초기 세팅으로 이동” 버튼을 누른다.

### Flow 2: HiCardi 초기 세팅

1. 사용자가 “초기 세팅” 탭을 연다.
2. 단계별 세팅 체크리스트를 확인한다.
3. 각 단계를 완료 체크한다.
4. 모든 필수 항목이 완료되면 “세팅 완료” 버튼이 활성화된다.
5. 버튼을 누르면 적용 환자 현황에 가상 환자가 추가된다.

### Flow 3: 적용 환자 현황 확인

1. 사용자가 “적용 환자 현황” 탭을 연다.
2. 더미 데이터 안내문을 확인한다.
3. 환자 리스트에서 상태를 확인한다.
4. 환자를 선택하여 상세 생체정보를 본다.
5. HR, RR, SpO2, EKG 시뮬레이션 그래프를 확인한다.
6. 모든 데이터가 더미 데이터임을 화면에서 확인할 수 있어야 한다.

### Flow 4: 작동 이상 Q&A 확인

1. 사용자가 “Q&A” 탭을 연다.
2. 문제 상황을 선택한다.
3. 임시 문제 해결 순서를 확인한다.
4. 해결되지 않으면 담당자 또는 관리자에게 문의하도록 안내한다.

---

## 📝 Content Placeholder Policy

아직 확정되지 않은 내용에는 다음과 같은 플레이스홀더를 사용한다.

```text
[임시 내용] 이 항목은 추후 병동 프로토콜과 제조사 지침을 바탕으로 수정 예정입니다.
```

```text
[추후 확정 예정] 실제 임상 기준은 아직 확정되지 않았습니다.
```

```text
[DEMO DATA] 본 생체정보는 실제 환자 데이터가 아닙니다.
```

---

## 🚫 Do Not

- 실제 환자 데이터처럼 보이게 만들지 말 것
- 더미 데이터임을 숨기지 말 것
- 체크리스트 결과를 확정적 의료 판단처럼 표현하지 말 것
- 실제 임상 기준이 확정된 것처럼 작성하지 말 것
- 제조사 공식 매뉴얼을 대체한다고 표현하지 말 것
- 환자 개인정보 저장 기능을 기본값으로 만들지 말 것
- 실제 HiCardi API 연동이 있는 것처럼 표현하지 말 것

---

## ✅ Final Expected Prototype

완성된 프로토타입은 다음을 보여줄 수 있어야 한다.

1. HiCardi 적용 기준을 체크리스트로 확인한다.
2. 체크된 항목에 따라 임시 점수가 자동 계산된다.
3. 임시 결과와 인계용 요약 문장이 생성된다.
4. 초기 세팅 절차를 단계별로 확인한다.
5. 간단 매뉴얼 항목을 카드형으로 확인한다.
6. 적용 환자 현황에서 더미 생체정보가 실시간처럼 표시된다.
7. HR, RR, SpO2, EKG 시뮬레이션 그래프가 보인다.
8. 모든 생체정보 화면에 더미 데이터임이 명시된다.
9. 작동 이상 시 Q&A를 통해 임시 해결 절차를 확인한다.
10. 앱 전체가 SD 프로젝트 발표용 시연 앱으로 사용할 수 있다.

---

## 📌 Final Note for Codex

이 프로젝트는 실제 의료기기 연동 앱이 아니라 **HiCardi 적용 기준과 운영 프로세스를 시각화하기 위한 간호관리학 SD 프로젝트용 프로토타입**이다.

따라서 구현 시 가장 중요한 것은 다음 세 가지이다.

1. **체크리스트 기반 적용 판단 흐름을 명확하게 보여줄 것**
2. **더미 생체정보 대시보드를 실제처럼 보이되, 더미 데이터임을 분명히 표시할 것**
3. **체크리스트·매뉴얼·Q&A의 세부 내용은 아직 확정되지 않았으므로 임시안으로 처리할 것**
